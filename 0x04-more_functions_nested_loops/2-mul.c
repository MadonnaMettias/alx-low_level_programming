#include "main.h"

/**
 * mul - a function that multiplies two integers.
 *@a: is a digit.
 *@b: is another digit.
 * Return: the result of multiplying a nd b.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
